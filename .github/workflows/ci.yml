# Toolbox CI (adapted from react-vite-starter config/github-workflows/ci.yml)

name: CI

on:
  pull_request:
    branches: ['*']
    paths-ignore:
      - '**.md'
      - 'LICENSE*'
      - '.gitignore'
      - '.vscode/**'

  push:
    branches: ['main']

  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install

      - name: Run tests
        run: npm run test:run
